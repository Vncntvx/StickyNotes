<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
        Info.plist for the StickyNotes app target.

        Most keys are set via INFOPLIST_KEY_* build settings in project.yml
        (Xcode 26.x modern practice). This file holds only the keys that
        cannot be expressed cleanly as INFOPLIST_KEY_* settings.
    -->
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>LSMinimumSystemVersion</key>
    <string>$(MACOSX_DEPLOYMENT_TARGET)</string>
    <!--
        LSUIElement: the app is menu-bar-primary. The Dock icon default-on
        behavior (FR-008) is controlled at runtime via activation policy
        (regular by default; user can switch to accessory). LSUIElement=NO
        would be the standard app; we set YES so the menu-bar library is the
        primary entry, and the Dock policy is a runtime user preference
        (R4).
    -->
    <key>LSUIElement</key>
    <true/>
    <key>NSHumanReadableCopyright</key>
    <string>Copyright © 2026 StickyNotes contributors. Licensed under the project license.</string>
    <key>NSPrincipalClass</key>
    <string>NSApplication</string>
    <!--
        URL scheme for deep links (plan §Widgets): note open, new, search.
        Placeholder scheme until final bundle id chosen.
    -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLName</key>
            <string>local.stickynotes.deeplink</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>stickynotes</string>
            </array>
        </dict>
    </array>
</dict>
</plist>
